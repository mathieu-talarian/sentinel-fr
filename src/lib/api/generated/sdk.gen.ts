// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { AdminRefreshTriggerData, AdminRefreshTriggerErrors, AdminRefreshTriggerResponses, AuthMeData, AuthMeErrors, AuthMeResponses, CatalogStatsData, CatalogStatsResponses, ChatData, ChatErrors, ChatResponses, ChatStreamData, ChatStreamErrors, ChatStreamResponse, ChatStreamResponses, ClassifyData, ClassifyErrors, ClassifyResponses, ConversationDeleteData, ConversationDeleteResponses, ConversationGetData, ConversationGetErrors, ConversationGetResponses, ConversationRenameData, ConversationRenameErrors, ConversationRenameResponses, ConversationsListData, ConversationsListResponses, DbInfoData, DbInfoErrors, DbInfoResponses, FrontendAlertsData, FrontendAlertsResponses, GetCodeData, GetCodeErrors, GetCodeResponses, HealthData, HealthResponses, HealthzData, HealthzResponses, ImportCaseAddLineItemData, ImportCaseAddLineItemErrors, ImportCaseAddLineItemResponses, ImportCaseCreateData, ImportCaseCreateErrors, ImportCaseCreateResponses, ImportCaseDeleteData, ImportCaseDeleteErrors, ImportCaseDeleteLineItemData, ImportCaseDeleteLineItemErrors, ImportCaseDeleteLineItemResponses, ImportCaseDeleteResponses, ImportCaseGetData, ImportCaseGetErrors, ImportCaseGetResponses, ImportCaseListData, ImportCaseListResponses, ImportCasePatchData, ImportCasePatchErrors, ImportCasePatchLineItemData, ImportCasePatchLineItemErrors, ImportCasePatchLineItemResponses, ImportCasePatchResponses, LandedCostData, LandedCostErrors, LandedCostResponses, RefreshStatusData, RefreshStatusErrors, RefreshStatusResponses, SearchData, SearchErrors, SearchResponses, WatchAlertsData, WatchAlertsResponses, WatchCheckData, WatchCheckErrors, WatchCheckResponses, WatchSubscribeData, WatchSubscribeErrors, WatchSubscribeResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Dispatch a manual catalog refresh.
 *
 * Returns 202 (Accepted) once the actor's mailbox has accepted the message;
 * the actual fetch + diff happens asynchronously, mirroring the behaviour of
 * the periodic ticks. Use `GET /db/refresh-status` to observe completion.
 *
 * Requires:
 * * a verified Firebase ID token via `Authorization: Bearer <token>`,
 * * `SENTINEL_REFRESH_ENABLED=1` at server boot — otherwise 503.
 */
export const adminRefreshTrigger = <ThrowOnError extends boolean = false>(options?: Options<AdminRefreshTriggerData, ThrowOnError>) => (options?.client ?? client).post<AdminRefreshTriggerResponses, AdminRefreshTriggerErrors, ThrowOnError>({ url: '/admin/refresh', ...options });

export const frontendAlerts = <ThrowOnError extends boolean = false>(options?: Options<FrontendAlertsData, ThrowOnError>) => (options?.client ?? client).get<FrontendAlertsResponses, unknown, ThrowOnError>({ url: '/alerts', ...options });

/**
 * Verify the caller's Firebase ID token, register the user in the local
 * database (idempotent — first call materialises the row), and return
 * the profile.
 *
 * Auth contract:
 * * Caller supplies `Authorization: Bearer <firebase_id_token>`.
 * * Token must be RS256-signed by Google with `iss=https://securetoken.google.com/<project_id>`,
 * `aud=<project_id>`, `exp` in the future, and `email_verified=true`
 * (relaxable via `FIREBASE_REQUIRE_VERIFIED_EMAIL=0`).
 * * 401 on missing/invalid token; 503 when the server has no
 * `FIREBASE_PROJECT_ID` configured.
 */
export const authMe = <ThrowOnError extends boolean = false>(options?: Options<AuthMeData, ThrowOnError>) => (options?.client ?? client).get<AuthMeResponses, AuthMeErrors, ThrowOnError>({ url: '/auth/me', ...options });

export const catalogStats = <ThrowOnError extends boolean = false>(options?: Options<CatalogStatsData, ThrowOnError>) => (options?.client ?? client).get<CatalogStatsResponses, unknown, ThrowOnError>({ url: '/catalog/stats', ...options });

/**
 * Send a chat turn to the Sentinel assistant. Returns the assistant's reply.
 *
 * The agent has access to the full Sentinel toolset and will call any
 * combination of tools needed. Wall-clock 5–30 s depending on tool fan-out.
 *
 * The reply's `message.content` is GitHub-flavored markdown intended to be
 * rendered by the frontend (bold codes, bullet caveats, comparison tables).
 */
export const chat = <ThrowOnError extends boolean = false>(options: Options<ChatData, ThrowOnError>) => (options.client ?? client).post<ChatResponses, ChatErrors, ThrowOnError>({
    url: '/chat',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Streaming counterpart of [`chat_endpoint`]. Returns `text/event-stream`
 * with one SSE event per `ChatChunk`. Frontend usage:
 *
 * ```js
 * const r = await fetch("/chat/stream", {
 * method: "POST",
 * headers: { "content-type": "application/json" },
 * body: JSON.stringify({ messages, provider, lang, conversationId }),
 * });
 * const reader = r.body.pipeThrough(new TextDecoderStream()).getReader();
 * // parse `data: <json>\n\n` events as they arrive.
 * ```
 *
 * Event payload shape (one of):
 * - `{"type":"delta","text":"..."}` — token delta; concatenate to render
 * reply. The final concatenated buffer is GitHub-flavored markdown —
 * render it through a markdown component, not as plain text.
 * - `{"type":"turnStart","messageId":"msg_...","conversationId":"conv_..."}` — server ids
 * - `{"type":"toolCall","callId":"...","name":"search_codes","args":{...}}` — agent invoked a tool
 * - `{"type":"toolResult","callId":"...","tool":"search_codes","content":{...}}` — tool finished
 * - `{"type":"toolError","callId":"...","message":"...","code":"upstreamError"}` — tool failed
 * - `{"type":"error","message":"..."}` — terminal error
 * - `{"type":"done"}` — terminal success marker
 */
export const chatStream = <ThrowOnError extends boolean = false>(options: Options<ChatStreamData, ThrowOnError, ChatStreamResponse>) => (options.client ?? client).sse.post<ChatStreamResponses, ChatStreamErrors, ThrowOnError>({
    url: '/chat/stream',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Classify a free-text product description into a 10-digit HTS code.
 *
 * Runs an LLM agent loop (Anthropic or `OpenAI`) using hybrid search +
 * CROSS rulings + landed-cost tools. Wall-clock 8-35s per agent budget.
 */
export const classify = <ThrowOnError extends boolean = false>(options: Options<ClassifyData, ThrowOnError>) => (options.client ?? client).post<ClassifyResponses, ClassifyErrors, ThrowOnError>({
    url: '/classify',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Look up a 10-digit HTS code, return the entry plus its ancestor chain.
 */
export const getCode = <ThrowOnError extends boolean = false>(options: Options<GetCodeData, ThrowOnError>) => (options.client ?? client).get<GetCodeResponses, GetCodeErrors, ThrowOnError>({ url: '/code/{code}', ...options });

export const conversationsList = <ThrowOnError extends boolean = false>(options?: Options<ConversationsListData, ThrowOnError>) => (options?.client ?? client).get<ConversationsListResponses, unknown, ThrowOnError>({ url: '/conversations', ...options });

export const conversationDelete = <ThrowOnError extends boolean = false>(options: Options<ConversationDeleteData, ThrowOnError>) => (options.client ?? client).delete<ConversationDeleteResponses, unknown, ThrowOnError>({ url: '/conversations/{id}', ...options });

export const conversationGet = <ThrowOnError extends boolean = false>(options: Options<ConversationGetData, ThrowOnError>) => (options.client ?? client).get<ConversationGetResponses, ConversationGetErrors, ThrowOnError>({ url: '/conversations/{id}', ...options });

export const conversationRename = <ThrowOnError extends boolean = false>(options: Options<ConversationRenameData, ThrowOnError>) => (options.client ?? client).patch<ConversationRenameResponses, ConversationRenameErrors, ThrowOnError>({
    url: '/conversations/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Confirms the `SurrealDB` connection is alive.
 */
export const dbInfo = <ThrowOnError extends boolean = false>(options?: Options<DbInfoData, ThrowOnError>) => (options?.client ?? client).get<DbInfoResponses, DbInfoErrors, ThrowOnError>({ url: '/db/info', ...options });

/**
 * Latest 5 catalog-refresh-worker runs, newest first.
 */
export const refreshStatus = <ThrowOnError extends boolean = false>(options?: Options<RefreshStatusData, ThrowOnError>) => (options?.client ?? client).get<RefreshStatusResponses, RefreshStatusErrors, ThrowOnError>({ url: '/db/refresh-status', ...options });

/**
 * Liveness check.
 */
export const health = <ThrowOnError extends boolean = false>(options?: Options<HealthData, ThrowOnError>) => (options?.client ?? client).get<HealthResponses, unknown, ThrowOnError>({ url: '/health', ...options });

/**
 * Liveness check, Kubernetes-convention alias. External monitoring tools
 * (GCP uptime checks, Sentry cron probes, etc.) default to `/healthz` —
 * keeping both `/health` (used by Cloud Run probes in cloudrun-service.yaml)
 * and `/healthz` (used by everything else) avoids 404s in dashboards.
 */
export const healthz = <ThrowOnError extends boolean = false>(options?: Options<HealthzData, ThrowOnError>) => (options?.client ?? client).get<HealthzResponses, unknown, ThrowOnError>({ url: '/healthz', ...options });

/**
 * List the caller's cases, newest-`updated_at` first. Cursor is the
 * previous page's last `updated_at,id` keyset marker.
 */
export const importCaseList = <ThrowOnError extends boolean = false>(options?: Options<ImportCaseListData, ThrowOnError>) => (options?.client ?? client).get<ImportCaseListResponses, unknown, ThrowOnError>({ url: '/import-cases', ...options });

/**
 * Create a new import case (optionally with initial line items).
 */
export const importCaseCreate = <ThrowOnError extends boolean = false>(options: Options<ImportCaseCreateData, ThrowOnError>) => (options.client ?? client).post<ImportCaseCreateResponses, ImportCaseCreateErrors, ThrowOnError>({
    url: '/import-cases',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a case. Line items and candidates cascade.
 */
export const importCaseDelete = <ThrowOnError extends boolean = false>(options: Options<ImportCaseDeleteData, ThrowOnError>) => (options.client ?? client).delete<ImportCaseDeleteResponses, ImportCaseDeleteErrors, ThrowOnError>({ url: '/import-cases/{caseId}', ...options });

/**
 * Get one case with its line items.
 */
export const importCaseGet = <ThrowOnError extends boolean = false>(options: Options<ImportCaseGetData, ThrowOnError>) => (options.client ?? client).get<ImportCaseGetResponses, ImportCaseGetErrors, ThrowOnError>({ url: '/import-cases/{caseId}', ...options });

/**
 * Patch a case header.
 */
export const importCasePatch = <ThrowOnError extends boolean = false>(options: Options<ImportCasePatchData, ThrowOnError>) => (options.client ?? client).patch<ImportCasePatchResponses, ImportCasePatchErrors, ThrowOnError>({
    url: '/import-cases/{caseId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Append a line item to a case.
 */
export const importCaseAddLineItem = <ThrowOnError extends boolean = false>(options: Options<ImportCaseAddLineItemData, ThrowOnError>) => (options.client ?? client).post<ImportCaseAddLineItemResponses, ImportCaseAddLineItemErrors, ThrowOnError>({
    url: '/import-cases/{caseId}/line-items',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a line item. Candidates cascade.
 */
export const importCaseDeleteLineItem = <ThrowOnError extends boolean = false>(options: Options<ImportCaseDeleteLineItemData, ThrowOnError>) => (options.client ?? client).delete<ImportCaseDeleteLineItemResponses, ImportCaseDeleteLineItemErrors, ThrowOnError>({ url: '/import-cases/{caseId}/line-items/{lineId}', ...options });

/**
 * Patch a line item.
 */
export const importCasePatchLineItem = <ThrowOnError extends boolean = false>(options: Options<ImportCasePatchLineItemData, ThrowOnError>) => (options.client ?? client).patch<ImportCasePatchLineItemResponses, ImportCasePatchLineItemErrors, ThrowOnError>({
    url: '/import-cases/{caseId}/line-items/{lineId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Compute the landed cost for a 10-digit HTS code (Sentinel V1: FR origin → US destination).
 */
export const landedCost = <ThrowOnError extends boolean = false>(options: Options<LandedCostData, ThrowOnError>) => (options.client ?? client).post<LandedCostResponses, LandedCostErrors, ThrowOnError>({
    url: '/landed-cost',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Hybrid lexical + vector search over the HTS catalog.
 */
export const search = <ThrowOnError extends boolean = false>(options: Options<SearchData, ThrowOnError>) => (options.client ?? client).post<SearchResponses, SearchErrors, ThrowOnError>({
    url: '/search',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const watchAlerts = <ThrowOnError extends boolean = false>(options?: Options<WatchAlertsData, ThrowOnError>) => (options?.client ?? client).get<WatchAlertsResponses, unknown, ThrowOnError>({ url: '/watch/alerts', ...options });

export const watchCheck = <ThrowOnError extends boolean = false>(options: Options<WatchCheckData, ThrowOnError>) => (options.client ?? client).post<WatchCheckResponses, WatchCheckErrors, ThrowOnError>({
    url: '/watch/check',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const watchSubscribe = <ThrowOnError extends boolean = false>(options: Options<WatchSubscribeData, ThrowOnError>) => (options.client ?? client).post<WatchSubscribeResponses, WatchSubscribeErrors, ThrowOnError>({
    url: '/watch/subscribe',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
